package pkg

import "udemy/pkg/models"

var Categories = make([]models.Category, 0)
var Regions = make([]models.Region, 0)
var Cities = make([]models.City, 0)
var Courses = make([]models.Course, 0)
var Users = make([]models.User, 0)
var Subscriptions = make([]models.Subscription, 0)
